<resources>

    <style name="Theme.GeoRent" parent="Theme.GeoRent.Base">

    </style>

    <!-- Tema da aplicação -->
    <style name="Theme.GeoRent.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="Theme.GeoRent.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.GeoRent.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
